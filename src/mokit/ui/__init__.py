"""Shared imgui_bundle UI kit (optional extra `ui`): style sheet, layout containers, widgets, and the generic
params editor / node editor over `mokit.graph` graphs. Apps re-export `style` and add their own constants."""
